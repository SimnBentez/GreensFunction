[build-system]
requires = ["scikit-build-core", "pybind11"]
build-backend = "scikit_build_core.build"

[project]
name = "shape-functions"
version = "0.1.0"

[tool.scikit-build]
wheel.expand-macos-universal-tags = true
install.components = ["python_modules"] # Opcional, dependiendo de tu CMake