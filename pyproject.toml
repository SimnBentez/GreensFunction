[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "BeamSolver"
version = "0.1.0"
description = "Cálculo rápido de funciones de forma y Green para vigas"
dependencies = [
    "numpy",
]

[tool.setuptools.packages.find]
where = ["."]