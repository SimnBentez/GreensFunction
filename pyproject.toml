[build-system]
requires = ["scikit-build-core", "pybind11"]
build-backend = "scikit_build_core.build"

[project]
name = "greensfunction"
version = "0.1.0"
description = "Green function beam solver"
authors = [{name="Simón Benítez Soto"}]

[tool.scikit-build]
build-dir = "build/{wheel_tag}"